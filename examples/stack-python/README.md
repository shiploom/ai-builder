# stack-python — minimal Python fixture (AC demo scaffold)

Tiny password-reset helper. Used by `scripts/ac-demo.sh` as a scratch
scaffold: copy to scratch, `shiploom init`, configure the `test` gate,
`shiploom verify`. (The demo's gates are shell-based since v1.3.0; the
bundled pytest suite is inert fixture data.)
