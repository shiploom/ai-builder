# examples/ — fixtures (double as conformance inputs)

- `greenfield-starter/` — idea + expected artifacts for AC1 demo.
- `brownfield-sample/` — legacy repo with known debt for AC2 demo.
- `stack-python/` / `stack-node/` — 2-stack matrix for MVP exit (AC1–AC6 on 2 stacks × 2 harnesses).
- `team-policy-pack/` — example team policy overlay (stricter migrations, actor-conditional deploys).
- `deploy/` — per-target starters: `docker/`, `k8s/`, `aws/`, `preview/`.
