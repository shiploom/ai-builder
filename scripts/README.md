# scripts/ — repo tooling (render, lint-spelling, check-links)

- `ac-demo.sh` — MVP acceptance demo (AC1–AC6, BUILD_PLAN PR10).
  Runs the real CLI in scratch dirs: `sh scripts/ac-demo.sh`
  (`SHIP_PY` overrides the interpreter; node leg skips if unavailable).
