{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://shiploom.dev/schemas/trace-link",
  "title": "Shiploom trace index (trace.json)",
  "description": "Validates trace.json generated by `validate`: Requirement -> Decision -> Implementation -> Test -> Verification Result links (MASTER_SPEC 16). Queryable via `shiploom status --json | jq .trace[\"REQ-001\"]`. Relation names form a closed set to catch typos.",
  "type": "object",
  "patternProperties": {
    "^[A-Z]{2,6}-[0-9]{3,}$": {
      "type": "object",
      "properties": {
        "requires": { "type": "array", "items": { "type": "string" } },
        "decided_by": { "type": "array", "items": { "type": "string" } },
        "implemented_by": { "type": "array", "items": { "type": "string" } },
        "tested_by": { "type": "array", "items": { "type": "string" } },
        "verified_by": { "type": "array", "items": { "type": "string" } }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
