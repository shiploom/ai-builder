# brownfield-sample — legacy fixture with known debt (AC2 demo)

A small user-listing module with an off-by-one pagination bug and
reflection-free, grep-able debt markers. The demo maps it, plans the
fix behind an approval, captures characterization tests, and verifies
the regression flow — without protected merges lacking approval.
