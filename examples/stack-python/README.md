# stack-python — minimal Python fixture (AC demo, 2-stack leg)

Tiny password-reset helper with a pytest suite. Used by `scripts/ac-demo.sh`:
copy to scratch, `shiploom init`, configure the `test` gate, `shiploom verify`.
