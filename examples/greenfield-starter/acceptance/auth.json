[
  {
    "id": "ACC-101",
    "statement": "Password reset email arrives within 60 seconds for valid accounts",
    "howToVerify": {
      "type": "script",
      "command": "pytest tests/e2e_reset.py -q",
      "expect": "exit 0 with delivery timestamp delta under 60s",
      "timeoutS": 300
    },
    "oracleRef": "oracle/ACC-101.sh"
  },
  {
    "id": "ACC-102",
    "statement": "Reset tokens are rejected when presented more than 15 minutes after issue",
    "howToVerify": {
      "type": "script",
      "command": "pytest tests/token_expiry.py -q",
      "expect": "exit 0 with late token rejected at the 15m boundary",
      "timeoutS": 300
    },
    "oracleRef": "oracle/ACC-102.sh"
  }
]
